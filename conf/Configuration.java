package conf;

/**
 * Configuration: contains static variables to configure the simulation.
 */
public class Configuration {
    /**
     * Total number of passengers in the simulation.
     */
    public static final int NUMBER_OF_PASSENGERS = 21;

    /**
     * Maximum number of passengers that a plane can hold.
     */
    public static final int MAX_PASSENGERS_PLANE = 10;

    /**
     * Minimum number of passengers that a plane can hold.
     */
    public static final int MIN_PASSENGERS_PLANE = 5;
}